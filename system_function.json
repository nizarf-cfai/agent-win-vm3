[
    {
        "name": "get_query",
        "description": "Extract detailed use query.",
        "parameters": {
            "type": "OBJECT",
            "properties": {
                "query": {
                    "type": "STRING",
                    "description": "User raw question or command."
                }
            },
            "required": ["query"]
        }
    }
]